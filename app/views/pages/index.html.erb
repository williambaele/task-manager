<!-- HERO PART-->
<% if user_signed_in? %>
  <%= render "shared/hero" %>
<% else %>
  <%= render "shared/hero-not-logged" %>
<% end %>

<!-- TASK PART -->
<% if user_signed_in? %>
<%= render "shared/done-tasks" %>

  <%= render "shared/my-tasks" %>
<% else %>
  <%= render "shared/no-tasks" %>
<% end %>
